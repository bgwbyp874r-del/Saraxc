// Home Page JavaScript Functions

// Smooth scroll to categories
function scrollToCategories() {\n    document.getElementById('categories').scrollIntoView({ behavior: 'smooth' });\n}\n\n// Show upload modal\nfunction showUploadModal() {\n    alert('Upload feature coming soon!');\n    // You can integrate a modal here\n}\n\n// Admin file access (requires authentication)\nfunction accessAdminFile(fileName) {\n    const isAdmin = localStorage.getItem('isAdmin');\n    if (!isAdmin) {\n        alert('Admin access required');\n        return;\n    }\n    alert('Accessing: ' + fileName);\n}\n\n// Search functionality\nfunction searchContent(query) {\n    console.log('Searching for:', query);\n    // Implement search logic here\n}\n\n// Category click handler\ndocument.querySelectorAll('.category-card a').forEach(link => {\n    link.addEventListener('click', function(e) {\n        e.preventDefault();\n        const categoryName = this.closest('.category-card').querySelector('h4').textContent;\n        console.log('Browsing category:', categoryName);\n        // Redirect to category page\n    });\n});\n\n// Actress follow handler\ndocument.querySelectorAll('.actress-card button').forEach(button => {\n    button.addEventListener('click', function() {\n        if (this.textContent.includes('Follow')) {\n            this.textContent = '✓ Following';\n            this.classList.remove('btn-outline-primary');\n            this.classList.add('btn-primary');\n        } else {\n            this.textContent = '+ Follow';\n            this.classList.remove('btn-primary');\n            this.classList.add('btn-outline-primary');\n        }\n    });\n});\n\n// Admin file access handler\ndocument.querySelectorAll('.admin-card button').forEach(button => {\n    button.addEventListener('click', function() {\n        const fileName = this.closest('.admin-card').querySelector('h5').textContent;\n        accessAdminFile(fileName);\n    });\n});\n\n// Community link handlers\ndocument.querySelectorAll('.community-card .btn').forEach(link => {\n    link.addEventListener('click', function(e) {\n        e.preventDefault();\n        const platform = this.closest('.community-card').querySelector('h5').textContent;\n        console.log('Joining:', platform);\n    });\n});\n\n// Initialize on page load\ndocument.addEventListener('DOMContentLoaded', function() {\n    console.log('Sara.xc Home Page Loaded');\n    \n    // Initialize animations\n    initializeAnimations();\n    \n    // Load categories\n    loadCategories();\n});\n\n// Initialize scroll animations\nfunction initializeAnimations() {\n    const observerOptions = {\n        threshold: 0.1,\n        rootMargin: '0px 0px -100px 0px'\n    };\n    \n    const observer = new IntersectionObserver(function(entries) {\n        entries.forEach(entry => {\n            if (entry.isIntersecting) {\n                entry.target.style.opacity = '1';\n                entry.target.style.transform = 'translateY(0)';\n            }\n        });\n    }, observerOptions);\n    \n    document.querySelectorAll('.category-card, .work-card, .admin-card, .actress-card').forEach(el => {\n        el.style.opacity = '0';\n        el.style.transform = 'translateY(20px)';\n        el.style.transition = 'opacity 0.5s ease, transform 0.5s ease';\n        observer.observe(el);\n    });\n}\n\n// Load categories data\nfunction loadCategories() {\n    const categories = [\n        { id: 1, name: 'Free Files', icon: 'fas fa-gift', color: '#3b82f6' },\n        { id: 2, name: 'Desi', icon: 'fas fa-heart', color: '#ff6b6b' },\n        { id: 3, name: 'Masala', icon: 'fas fa-fire', color: '#ffd700' },\n        { id: 4, name: 'TV Shows', icon: 'fas fa-tv', color: '#00d4ff' },\n        { id: 5, name: 'Films', icon: 'fas fa-film', color: '#ff1493' },\n        { id: 6, name: 'Web Series', icon: 'fas fa-play-circle', color: '#9d4edd' }\n    ];\n    \n    console.log('Categories loaded:', categories);\n}\n\n// Search handler\nconst searchInput = document.querySelector('input[type=\"email\"]');\nif (searchInput) {\n    searchInput.addEventListener('keypress', function(e) {\n        if (e.key === 'Enter') {\n            searchContent(this.value);\n        }\n    });\n}\n\n// Navbar active state\nwindow.addEventListener('scroll', function() {\n    const navbar = document.querySelector('.navbar-custom');\n    if (window.scrollY > 50) {\n        navbar.style.boxShadow = '0 4px 20px rgba(0, 0, 0, 0.5)';\n    } else {\n        navbar.style.boxShadow = '0 4px 20px rgba(0, 0, 0, 0.3)';\n    }\n});\n\n// Mobile menu close on link click\ndocument.querySelectorAll('.nav-link').forEach(link => {\n    link.addEventListener('click', function() {\n        const navbarToggle = document.querySelector('.navbar-toggler');\n        if (navbarToggle.offsetParent !== null) { // Check if visible\n            navbarToggle.click();\n        }\n    });\n});\n\n// Export functions for use in other files\nwindow.scrollToCategories = scrollToCategories;\nwindow.showUploadModal = showUploadModal;\nwindow.accessAdminFile = accessAdminFile;\nwindow.searchContent = searchContent;